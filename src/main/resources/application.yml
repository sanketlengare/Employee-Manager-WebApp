spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/dbmsProject
    username: root
    password: adminuser
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect